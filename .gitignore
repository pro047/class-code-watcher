# 파이프라인 작업 산출물 (세션마다 새로 생긴다)
.pipeline/

# 도구가 만드는 세션 기록 (PRD 9.1)
sessions/

# 비밀값 — 절대 커밋하지 않는다 (PRD 13.1)
.env
!.env.example

# 파이썬
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# 실험 요약 산출물 (세션마다 새로 생긴다)
experiments/summary_v2/out/

# 단계: 판단 검증

기능: $FEATURE

설계 문서의 **주장**을 감사한다. 설계를 고치는 단계가 아니고, 구현 품질을 보는
단계도 아니다. **"이 문서가 사실이라고 말한 것들이 실제로 사실인가"** 하나만 본다.

이 단계가 있는 이유: 구현물은 테스트·리뷰·게이트를 통과하는데 **판단물(원인 판정,
우선순위, "X가 없다")은 아무 검증 없이 다음 단계로 흘러간다.** 틀린 1순위는 틀린
코드보다 비싸다 — 며칠을 엉뚱한 곳에 쓰게 만든다.

## 출력 (이 파일 하나만 쓴다)
$WORK/JUDGE.md

## 입력 (읽기만, 절대 수정 금지)
$WORK/DESIGN.md

## 할 일

1. `$WORK/DESIGN.md` 에서 **하중을 받는 주장**을 전부 뽑는다. 하중을 받는다 =
   그게 틀리면 설계의 방향·순서·범위가 바뀐다.
   - 원인 판정 ("느린 이유는 N+1 쿼리다")
   - 우선순위·순서 근거 ("A 를 먼저 해야 한다")
   - 부재 주장 ("이 기능은 없다", "로깅이 없다", "테스트가 없다")
   - 현재 동작 주장 ("이 API 는 500 을 반환한다")
   - 비용·성능·보안 주장 (숫자를 동반하는 것)
   - 외부 스펙·라이브러리 동작 주장

   설계 취향(네이밍, 파일 배치)이나 아직 만들지 않은 것에 대한 계획은 대상이 아니다.

2. 각 주장에 **근거 등급**을 매긴다.
   - `실측` — 도구를 실제로 돌린 출력이 근거
   - `코드확인` — `파일:줄` 로 짚을 수 있음
   - `추정` — 위 둘이 아닌 전부 (관례·유추·문서만 읽음·그럴듯함)

3. **확인이 가능한 주장은 직접 확인한다.** 이게 이 단계의 본론이다.
   `$ROOT` 에서 grep/테스트/스크립트를 돌려 재현한다. 확인 비용이 도구 호출
   1~2회면 반드시 한다. 문서만 읽고 넘기지 않는다.
   - 부재 주장은 **전수 확인**한다. 서비스·언어가 여러 개면 전부 본다
     (한쪽만 grep 하고 "없다"고 한 것이 실제로 자주 나오는 오류다)
   - 숫자 주장은 산수를 해본다. 자릿수가 맞는지만 봐도 죽는 주장이 많다

4. 확인 결과를 판정으로 적는다.
   - `확인` — 주장이 맞다 (재현 방법·출력 요약을 남긴다)
   - `반박` — 주장이 틀리다 (**무엇이 실제인지**와 근거를 남긴다)
   - `미확인` — 이 단계에서 확인 못 했다 (**왜** 못 했는지, 누가 무엇을 하면
     확인되는지 남긴다)

## 이 저장소에서 특히 자주 틀리는 주장

아래는 등급이 `추정` 으로 떨어지기 쉬운데 확인 비용이 도구 호출 1~2회인 것들이다.
설계가 이런 주장을 했으면 **반드시 직접 확인한다.**

- **외부 API 표면 주장** — OpenAI 요청/응답 필드명, Discord Webhook payload 필드는
  **저장소 안에 정본이 없다.** 근거 없이 단정했으면 `미확인` 이다. 더불어 설계가
  이걸 어댑터 뒤로 격리하지 않고 여러 곳에 흩뿌렸으면 그 구조 자체를 지적한다 —
  틀렸을 때 고칠 자리가 몇 군데인지가 이 주장의 하중이다.
- **표준 라이브러리 동작 주장** — `difflib`·`pathlib`·`hashlib`·`watchdog` 같은 것은
  `$PY -c '...'` 한 번이면 확인된다. **확인 비용이 낮으므로 `미확인` 으로 남기지 마라.**
- **"이 기능은 아직 없다" 류의 부재 주장** — `$ROOT/src` 전체를 본다.
  PRD.md 의 요구사항 목록은 **근거가 아니다** — 문서에 안 적혔는데 코드는 있는
  경우가 실제로 있다. 코드를 봐라.
- **"이미 구현돼 있다" 주장** — 반대 방향도 같다. 파일이 존재한다는 것과 그 함수가
  실제로 호출된다는 것은 다르다. 호출부까지 grep 해서 `파일:줄` 로 짚는다.
- **PRD 인용 주장** — 설계가 "FR-xxx 에 따르면" 이라고 쓴 것은 `$ROOT/PRD.md` 를
  열어 대조한다. ID 를 잘못 짚거나 수용 기준을 느슨하게 바꿔 인용하는 일이 나온다.
- **실행 결과 주장** — `.env` 가 없거나 더미 값이고 의존성이 안 깔려 있을 수 있다.
  OpenAI·디스코드에 실제로 붙어봤다는 주장은 이 환경에서 성립할 수 없다.
  설계에 그런 근거가 적혀 있으면 반박한다.
- **테스트·타입 검사 주장** — 돌려봤다고 쓰여 있으면 실제로 돌려서 확인한다.
  `node_modules` 가 없어 못 돌렸으면 그 사실과 함께 `미확인`.

## JUDGE.md 필수 형식

첫 줄은 STATUS 라인. 그 다음 줄에 **기계가 읽는 카운트 한 줄**을 정확히 이 형식으로:

```
STATUS: DONE
UNVERIFIED: <미확인 개수> REFUTED: <반박 개수>
```

파이프라인 셸이 이 숫자를 파싱한다. **하나라도 0 이 아니면 사람 게이트가 뜬다**
(무인 실행이어도 멈춘다). 숫자를 낮게 쓰려고 판정을 무르게 하지 말 것 — 그건
이 단계를 무의미하게 만드는 유일한 방법이다. 애매하면 `미확인` 으로 센다.

이어서 주장별 표를 쓴다.

| # | 주장 (원문 인용) | 등급 | 판정 | 근거 / 실제 |
|---|---|---|---|---|

마지막에 다음 두 절을 반드시 쓴다.

- **구현 전에 해소해야 할 것** — `반박` 과, 방향을 바꿀 수 있는 `미확인`.
  각 항목에 "무엇을 하면 풀리는지" 를 적는다
- **그대로 진행해도 되는 것** — 틀려도 손해가 작은 `미확인`. 왜 작은지 한 줄

## 근거는 저장소 안에서만 찾는다

**외부 문서를 가져오지 않는다** (WebFetch·WebSearch). 두 가지 이유다.

- **재현성** — 외부 문서는 주행마다 다른 답을 준다. 같은 설계를 두 번 판정하면
  두 번 다 같은 결론이 나와야 판정이 근거로 쓰인다.
- **비용** — 외부 문서는 길고, 이 단계는 예산이 먼저 닿는 단계다
  (2026-08-24 실측: judge 가 \$5.08 에서 예산 초과로 죽었다).

표준 라이브러리·설치된 패키지의 동작은 `$PY -c '...'` 로 직접 돌려 확인한다 —
그게 이 저장소 안의 정본이다. 외부 API 표면처럼 저장소 안에 정본이 없는 것은
추측하지 말고 `미확인` 으로 남긴다.

**명령은 잘게 나눠 실행한다.** 헤드리스 실행이라 권한을 물어볼 사람이 없고,
복합 명령(`a && b`, `a; b`)은 조각 하나가 허용 밖이면 **전체가 거부된다** —
우회하느라 턴을 태우게 된다. 한 번에 하나씩 돌려라.

## 금지

- **설계 문서를 수정하지 않는다.** 틀린 게 있으면 JUDGE.md 에 적을 뿐이다
- **코드를 수정하지 않는다.** 확인을 위한 읽기·실행만 한다. 확인용 임시 파일을
  만들었으면 지운다
- 주장이 없는데 억지로 채우지 않는다. 하중받는 주장이 0 개면 그렇게 쓴다
- **"통과했습니다" 로 끝내지 않는다.** 확인하지 않은 것을 `확인` 으로 적는 것이
  이 단계의 유일한 실패 모드다. 확인 못 했으면 `미확인` 이 정답이다
- 설계 자체가 읽을 수 없거나 하중받는 주장을 판별할 수 없으면 `STATUS: BLOCKED`
